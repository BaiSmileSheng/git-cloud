<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.system.mapper.CdMaterialExtendInfoMapper">

    <sql id="Base_Column_List">
            id,
            material_code,
            material_desc,
            customer_name,
            product_type,
            life_cycle,
            is_putting_out,
            is_zn_attestation,
            is_get_stock,
            establish_date,
            del_flag,
            remark,
            create_time,
            create_by,
            update_time,
            update_by
    </sql>
    <resultMap type="CdMaterialExtendInfo" id="CdMaterialExtendInfoResult">
        <result property="id" column="id"/>
        <result property="materialCode" column="material_code"/>
        <result property="materialDesc" column="material_desc"/>
        <result property="customerName" column="customer_name"/>
        <result property="productType" column="product_type"/>
        <result property="lifeCycle" column="life_cycle"/>
        <result property="isPuttingOut" column="is_putting_out"/>
        <result property="isZnAttestation" column="is_zn_attestation"/>
        <result property="isGetStock" column="is_get_stock"/>
        <result property="establishDate" column="establish_date"/>
        <result property="delFlag" column="del_flag"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateBy" column="update_by"/>
    </resultMap>

    <sql id="selectCdMaterialExtendInfoVo">
        select id,
               material_code,
               material_desc,
               customer_name,
               product_type,
               life_cycle,
               is_putting_out,
               is_zn_attestation,
               is_get_stock,
               establish_date,
               del_flag,
               remark,
               create_time,
               create_by,
               update_time,
               update_by
        from cd_material_extend_info
    </sql>

<!--auto generated by MybatisCodeHelper on 2020-06-16-->
    <select id="selectMaterialCodeByLifeCycle" resultType="java.lang.String">
        select material_code
        from cd_material_extend_info
        where life_cycle=#{lifeCycle}
    </select>
    <select id="selectByMaterialCodeList" resultMap="CdMaterialExtendInfoResult" parameterType="list">
        <include refid="selectCdMaterialExtendInfoVo"/>
        <where>
            material_code in
            <foreach collection="list" item="item" open="(" close=")" separator="," index="index">
                #{item.productMaterialCode}
            </foreach>
        </where>
    </select>

    <select id="selectInfoInMaterialCodes" resultMap="CdMaterialExtendInfoResult">
        <include refid="selectCdMaterialExtendInfoVo"/>
        where
        del_flag='0' and
        material_code in
        <foreach collection="materialCodes" item="materialCode" open="(" separator="," close=")">
            #{materialCode}
        </foreach>
    </select>

<!--auto generated by MybatisCodeHelper on 2020-06-23-->
    <select id="selectOneByMaterialCode" resultMap="CdMaterialExtendInfoResult">
        select
        <include refid="Base_Column_List"/>
        from cd_material_extend_info
        where material_code=#{materialCode}
        limit 1
    </select>
    <insert id="batchInsertOrUpdate" parameterType="list">
        insert into cd_material_extend_info (
        material_code,
        material_desc,
        product_type,
        life_cycle,
        is_putting_out,
        is_zn_attestation,
        is_get_stock,
        establish_date,
        del_flag,
        remark,
        create_by,
        update_by,
        update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.materialCode},
            #{item.materialDesc},
            #{item.productType},
            #{item.lifeCycle},
            #{item.isPuttingOut},
            #{item.isZnAttestation},
            #{item.isGetStock},
            #{item.establishDate},
            ifnull(#{item.delFlag}, '0'),
            ifnull(#{item.remark}, ''),
            ifnull(#{item.createBy}, ''),
            ifnull(#{item.updateBy}, ''),
            now()
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        material_code = VALUES(material_code),
        material_desc = VALUES(material_desc),
        product_type = VALUES(product_type),
        life_cycle = VALUES(life_cycle),
        is_putting_out = VALUES(is_putting_out),
        is_zn_attestation = VALUES(is_zn_attestation),
        is_get_stock = VALUES(is_get_stock),
        establish_date = VALUES(establish_date),
        del_flag = VALUES(del_flag),
        remark = VALUES(remark),
        update_by = VALUES(update_by),
        update_time = now()
    </insert>
    <insert id="batchMaterialInsertOrUpdate" parameterType="list">
        insert into cd_material_extend_info (
        material_code,
        material_desc,
        product_type,
        life_cycle,
        is_putting_out,
        is_zn_attestation,
        is_get_stock,
        establish_date,
        del_flag,
        remark,
        create_by,
        update_by,
        update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.materialCode},
            #{item.materialDesc},
            #{item.productType},
            #{item.lifeCycle},
            #{item.isPuttingOut},
            #{item.isZnAttestation},
            #{item.isGetStock},
            #{item.establishDate},
            ifnull(#{item.delFlag}, '0'),
            ifnull(#{item.remark}, ''),
            ifnull(#{item.createBy}, ''),
            ifnull(#{item.updateBy}, ''),
            now()
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        material_code = VALUES(material_code),
        material_desc = VALUES(material_desc),
        del_flag = VALUES(del_flag),
        remark = VALUES(remark),
        update_by = VALUES(update_by),
        update_time = now()
    </insert>
</mapper>
